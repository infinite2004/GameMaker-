/// Create event for spikes_obj

fall_timer = 0;  // Timer to control when spikes fall
fall_interval = 0;  // Interval between falls, to be set based on the room

vspd = 0;        // Vertical speed
grav_amt = 0.5;  // Gravity or fall speedeed