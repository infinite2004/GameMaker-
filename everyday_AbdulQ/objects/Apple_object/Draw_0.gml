/// @description Custom draw to enlarge the sprite

// Draw the sprite with a scale factor of 2 (double the size)
//draw_sprite_ext(sprite_index, image_index, x, y, 3, 3, 0, c_white, 1);
draw_self();  // Draws the object's sprite at its current position

/// @description Draw the apple object with scale and juice effects

// Apply scaling to the apple


// Draw particles (optional)
part_system_drawit(particle_system);